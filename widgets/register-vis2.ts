import TrashScheduleWrapper from './TrashSchedule.vis2.wrapper.vue';

export const widgets = {
    TrashSchedule: {
        component: TrashScheduleWrapper,
        label: 'Trash Schedule',
        group: 'custom',
    },
};

export default widgets;
